[
 {kernel,
  [{logger,
    [{handler, default, logger_std_h,
            #{config => #{file => "log/error.log",
                          level => error}}}]}]},
 {pgo,
  [{pools,
    [{default, #{pool_size => 5,
                 host => "127.0.0.1",
                 database => "usersdb",
                 user => "restadmin",
                 password => "r35t@dm1n"}}]}]}
].