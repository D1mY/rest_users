services:
  db:
    image: postgres:alpine
    environment:
      POSTGRES_USER: restadmin
      POSTGRES_PASSWORD: r35t@dm1n
      POSTGRES_DB: usersdb_test
      PGDATA: "/var/lib/postgresql/data/pgdata"
    volumes:
      - ../config/sql:/docker-entrypoint-initdb.d
      # - ./_dbfiles:/var/lib/postgresql/data
    ports:
      - 5433:5432
    restart: always