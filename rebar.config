{erl_opts, [debug_info]}.
{deps, [cowboy, thoas, pgo]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [rest_users]},
    {config, "config/pgo.config"}
]}.

{relx, [{release, {rest_users, {git, short}},
  [rest_users]}
  ]}.
